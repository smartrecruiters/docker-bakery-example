FROM anapsix/alpine-java:8u144b01_jdk

# BAKERY* variables defined separately
# useful when you wish to have only few of them
ENV BAKERY_BUILDER_NAME="{{.BAKERY_BUILDER_NAME}}" \
	BAKERY_BUILDER_EMAIL="{{.BAKERY_BUILDER_EMAIL}}" \
	BAKERY_BUILDER_HOST="{{.BAKERY_BUILDER_HOST}}" \
	BAKERY_BUILD_DATE="{{.BAKERY_BUILD_DATE}}" \
	BAKERY_IMAGE_HIERARCHY="{{.BAKERY_IMAGE_HIERARCHY}}"
